#Serverconfigs
server.port= 7070
#server.contextPath=/api
#spring.data.rest.basePath=/api "Exposing Spring data repositories, not restcontrollers


#DBco
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver
#spring.datasource.url=jdbc:mysql://localhost:3306/twitchpow
#spring.datasource.username=root
#spring.datasource.password=test123
#spring.jdbc.validationQuery=select 1
#spring.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.naming_strategy=org.hibernate.cfg.EJB3NamingStrategy
#
#log4j.category.org.springframework.beans.factory=DEBUGnfigs
#
spring.datasource.url = jdbc:mysql://localhost:3306/arsdb
spring.datasource.username = root
spring.datasource.password = flatterhoden123!

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
#spring.jpa.hibernate.naming.strategy= org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect